{
  "name": "node-wechat-gpt",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev --turbopack",
    "build": "next build",
    "start": "next start",
    "lint": "next lint",
    "pages:build": "npx @cloudflare/next-on-pages",
    "pages:deploy": "npm run pages:build && wrangler pages deploy .vercel/output/static",
    "pages:dev": "npx wrangler pages dev --compatibility-date=2024-07-01 --compatibility-flags=nodejs_compat .vercel/output/static"
  },
  "dependencies": {
    "@google/generative-ai": "^0.24.0",
    "next": "^15.2.3",
    "openai": "^4.87.3",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "fast-xml-parser": "^4.5.0"
  },
  "devDependencies": {
    "@cloudflare/next-on-pages": "^1.13.6",
    "@tailwindcss/postcss": "^4",
    "tailwindcss": "^4",
    "wrangler": "^3.99.0"
  },
  "volta": {
    "node": "22.14.0"
  }
}